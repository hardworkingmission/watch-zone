import React from 'react';

const Answer3 = () => {
    return (
        <div>
            <p>useState is a build in function in react.It is called Hook.It can be used only in funcational component.useState creates the functional component statefull.It works just similler to class component state.It returns two values-a current state like as this.state and a function like as this.setstate that updates the value of the current state.</p>
        </div>
    );
};

export default Answer3;